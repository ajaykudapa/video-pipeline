# Copy to .env and adjust if needed
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
# Host:port the *browser* uses to reach MinIO for presigned download URLs
MINIO_PUBLIC_ENDPOINT=localhost:9000
